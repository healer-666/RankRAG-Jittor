--- external/JittorLLMs/models/qwen2/qwen2_jt/modeling_qwen2.py.backup_before_fp32_attention	2026-06-21 13:20:41.808211730 +0800
+++ external/JittorLLMs/models/qwen2/qwen2_jt/modeling_qwen2.py	2026-06-21 13:20:48.456195408 +0800
@@ -39,6 +39,13 @@
     if is_causal is None:
         is_causal = causal_mask is None and query.shape[2] > 1

+    # Qwen2.5-1.5B can produce non-finite FP16 attention outputs in
+    # Jittor SDPA. Compute attention in FP32, then cast back.
+    output_dtype = query.dtype
+    query = query.float()
+    key = key.float()
+    value = value.float()
+
     attn_output = jt.attention.scaled_dot_product_attention(
         query,
         key,
@@ -48,6 +55,7 @@
         scale=scaling,
         is_causal=is_causal,
     )
+    attn_output = attn_output.to(output_dtype)
     attn_output = attn_output.transpose(1, 2).contiguous()

     return attn_output, None
